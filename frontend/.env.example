# Frontend Environment Configuration

# Set to 'true' to use relay server instead of direct backend connection
VITE_USE_RELAY_SERVER=false

# Relay server URL (only used when VITE_USE_RELAY_SERVER=true)
VITE_RELAY_SERVER_URL=http://98.70.88.219:3001

# Example for production relay server
# VITE_RELAY_SERVER_URL=https://your-relay-server.example.com